<template>
  {{type}}
</template>
<script>
  export default {
   async asyncData({ params, $axios }) {
     const id = params.id
     const type = params.type
     return { id, type };
   },
</script>
